# Active in tests (Surefire sets spring.profiles.active=test). Demo data in
# classpath:db/seed must never reach the test database, so the test profile keeps
# Flyway to the schema migrations only - tests build their own fixtures via the API.
spring.flyway.locations=classpath:db/migration
